# ActaLog Docker Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# GitHub Container Registry
# =============================================================================
GITHUB_OWNER=yourusername
TAG=latest

# =============================================================================
# Application Configuration
# =============================================================================
PORT=8080
APP_URL=http://localhost:8080
CORS_ORIGINS=http://localhost:8080

# =============================================================================
# Database Configuration
# =============================================================================
# SQLite (default, recommended for single-server deployments)
DB_DRIVER=sqlite3
DB_NAME=/app/data/actalog.db

# PostgreSQL (uncomment and configure if using PostgreSQL)
# DB_DRIVER=postgres
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=actalog
# DB_USER=actalog
# DB_PASSWORD=your_secure_password_here

# MySQL/MariaDB (uncomment and configure if using MySQL)
# DB_DRIVER=mysql
# DB_HOST=mysql
# DB_PORT=3306
# DB_NAME=actalog
# DB_USER=actalog
# DB_PASSWORD=your_secure_password_here

# =============================================================================
# JWT Configuration
# =============================================================================
# IMPORTANT: Change this secret to a random secure value!
JWT_SECRET=change_this_to_a_secure_random_string_for_production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# =============================================================================
# Email Configuration (Optional - for password reset and verification)
# =============================================================================
EMAIL_FROM=noreply@yourdomain.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# =============================================================================
# OpenTelemetry Configuration (Optional - for observability)
# =============================================================================
# OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318

# =============================================================================
# Automatic Seed Import (Optional - for first deployment)
# =============================================================================
# Set these to automatically import movements and WODs from CSV files on first run
# The first user you create in the UI should match these credentials
# Leave commented out to skip automatic import (you can import manually via UI)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your_secure_admin_password
