# Harvested Features and Roadmap Summary

This page was generated by harvesting the repository README, CHANGELOG, and TODO files to surface the application's key features, recent releases, and high-priority work items so they can be turned into marketing and documentation content.

## Project Summary

- Name: ActaLog
- Purpose: Mobile-first fitness tracker for CrossFit / functional fitness athletes to log workouts, track progress, and analyze performance.
- Tech: Go backend, Vue.js frontend (PWA-capable), Docker deployment options.

## Core Features (from README)

- Workout logging (quick-log + detailed workout entries)
- Workout templates and composite templates (reusable WODs and movements)
- Performance tracking and charts (PRs, weight/metric trends)
- Calendar and timeline views for workout history
- Mobile-first responsive design and PWA support for offline recording
- Import/Export and seed data for common movements/WODs
- Multiple DB support (SQLite default, PostgreSQL, MariaDB)
- Docker images and deployment scripts for production

## Recent Notable Releases (top items from CHANGELOG)

### v0.14.0-beta — Subscription Billing System (2024-12-16)

- Dual-level subscription support: user and organization subscriptions
- Admin endpoints and UI required (create/mark-paid/cancel/view history)
- Read-only enforcement when subscriptions expire (HTTP 402 for write operations)
- DB migrations and multi-db snapshots included (SQLite/Postgres/MariaDB)

### v0.12.2-beta — PWA Offline Functionality (2025-11-28)

- Robust offline recording, improved axios offline detection and sync queue
- User-controlled PWA updates with an `UpdatePrompt.vue` component
- Offline save notifications and reliable PUT support in offline queue

### v0.12.0-beta — Mobile PWA Layout fixes (2025-11-26)

- Fixed overflow issues across many mobile views and added safe-area handling
- Consistent `.mobile-view-wrapper` layout pattern and CSS updates

## High-priority Backlog (extracted from TODO.md)

### Subscription-related (backend implemented — frontend pending)

- Frontend subscription status display on user profile/settings
- Admin subscription management UI (list, create, mark-paid, cancel)
- Read-only mode UI: banners, disabled controls, and graceful handling of HTTP 402

### Testing & Quality

- Add handler, service, and repository unit tests (high priority)
- Re-enable strict `golangci-lint` once issues are resolved

## Suggested Content Pieces to Create

- Feature page: "Offline-first PWA — record workouts offline" with screenshots and sync explanation
- Feature page: "Subscriptions & Admin billing" explaining dual-level model and read-only mode
- How-to / guide: "Deploy with Docker and external DB" (cover PostgreSQL and MariaDB notes)
- Roadmap summary highlighting next-priority items (Subscription UI, Admin UX, testing)

## Next Steps (what I can do now)

1. Extract more detailed feature copy per section (turn bullets into prose + examples).
2. Create individual site pages under `site/pages/` for the highest-priority features above.
3. Capture authenticated UI screenshots for admin/subscription flows (requires credentials and permission).
4. Generate short release-note-style summaries for the site using CHANGELOG entries.

If you'd like, I can proceed to expand any of the suggested content pieces into full pages and wire them into the static site scaffold under `site/`.
